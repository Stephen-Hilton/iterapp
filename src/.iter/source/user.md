# Source instructions: user

This work item was created directly by a human user.

- Trust the request as stated. The mainwork prompt is the spec; do not second-guess its
  intent or expand its scope.
- You are running autonomously — no one can answer questions mid-run. When the prompt is
  ambiguous, choose the most reasonable interpretation, proceed, and **document the
  choice explicitly in your output** so the user can correct it afterward.
- If the request is impossible as written (missing files, contradictory requirements),
  do as much as can be done safely, and report precisely what blocked the rest.
